<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Bids - Admin - Propload</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
</head>

<body class="bg-gray-50">
    <%- include('../partials/header1') %>

        <div class="max-w-7xl mx-auto px-4 py-8">
            <%- include('../partials/flash-messages1') %>

                <div class="flex justify-between items-center mb-8">
                    <div>
                        <h1 class="text-3xl font-bold text-gray-900">All Bids</h1>
                        <p class="text-gray-600 mt-2">Monitor all bids across the system</p>
                    </div>
                </div>

                <div class="bg-white shadow rounded-lg overflow-hidden">
                    <div class="overflow-x-auto">
                        <table class="min-w-full divide-y divide-gray-200">
                            <thead class="bg-gray-50">
                                <tr>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Project</th>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Seller</th>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Amount</th>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Status</th>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Submitted</th>
                                    <th
                                        class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                                        Actions</th>
                                    
                                </tr>
                            </thead>
                            <tbody class="bg-white divide-y divide-gray-200">
                                <% bids.forEach(bid=> { %>
                                    <tr class="hover:bg-gray-50">
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="text-sm font-medium text-gray-900">
                                                <% if (bid.project) { %>
                                                    <a href="/admin/projects/<%= bid.project._id %>"
                                                        class="text-indigo-600 hover:text-indigo-900">
                                                        <%= bid.project.title %>
                                                    </a>
                                                    <% } else { %>
                                                        <span class="text-gray-400">Project Deleted</span>
                                                        <% } %>
                                            </div>
                                            <% if (bid.project) { %>
                                                <div class="text-sm text-gray-500">
                                                    <%= bid.project.category %>
                                                </div>
                                                <% } %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <div class="text-sm text-gray-900">
                                                <%= bid.seller.name %>
                                            </div>
                                            <div class="text-sm text-gray-500">
                                                <%= bid.seller.companyName || 'No Company' %>
                                            </div>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                            $<%= bid.amount.toLocaleString() %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap">
                                            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full 
                                    <%= bid.status === 'submitted' ? 'bg-yellow-100 text-yellow-800' : '' %>
                                    <%= bid.status === 'won' ? 'bg-green-100 text-green-800' : '' %>
                                    <%= bid.status === 'lost' ? 'bg-red-100 text-red-800' : '' %>
                                    <%= bid.status === 'completed' ? 'bg-blue-100 text-blue-800' : '' %>">
                                                <%= bid.status %>
                                            </span>
                                            <% if (bid.autoWon) { %>
                                                <span class="ml-1 text-xs text-green-600" title="Auto-won by system">
                                                    <i class="fas fa-robot"></i>
                                                </span>
                                                <% } %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                            <%= moment(bid.createdAt).format('MMM D, YYYY') %>
                                        </td>
                                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium">
                                            <a href="/admin/projects/<%= bid.project._id %>"
                                                class="text-indigo-600 hover:text-indigo-900">
                                                <i class="fas fa-eye"></i> View Project
                                            </a>
                                        </td>
                                        
                                    </tr>
                                    <% }); %>
                            </tbody>
                        </table>
                    </div>
                </div>

                <% if (bids.length===0) { %>
                    <div class="bg-white shadow rounded-lg p-8 text-center">
                        <i class="fas fa-gavel text-gray-400 text-4xl mb-4"></i>
                        <h3 class="text-lg font-medium text-gray-900 mb-2">No Bids Found</h3>
                        <p class="text-gray-500">There are no bids in the system yet.</p>
                    </div>
                    <% } %>
        </div>
</body>

</html>