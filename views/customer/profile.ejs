<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Profile - Propload</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <style>
        :root {
            --propload-primary: #e9e2b3;
            --propload-secondary: #2f6c7b;
            --surface: #ffffff;
            --muted: #6b7280;
            --card-shadow: 0 8px 20px rgba(47, 76, 86, 0.06);
        }

        body {
            font-family: "Inter", sans-serif;
            background-color: #f9fafb;
            color: var(--muted);
            display: flex;
            min-height: 100vh;
        }

        main {
            flex: 1;
            padding: 2rem 1rem;
            margin-left: 260px;
            transition: margin-left 0.3s ease;
        }

        @media (max-width: 1024px) {
            main {
                margin-left: 0;
                padding: 1.5rem;
            }
        }

        .text-primary {
            color: var(--propload-primary);
        }

        .text-secondary {
            color: var(--propload-secondary);
        }

        .bg-primary {
            background-color: var(--propload-primary);
        }

        .bg-secondary {
            background-color: var(--propload-secondary);
        }

        .card {
            background: var(--surface);
            border-radius: 1.25rem;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
            border: 1px solid transparent;
        }

        .card:hover {
            transform: translateY(-6px);
            border-color: var(--propload-secondary);
            box-shadow: 0 12px 28px rgba(47, 108, 123, 0.15);
        }

        .icon-circle {
            width: 5rem;
            height: 5rem;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: var(--propload-primary);
            color: var(--propload-secondary);
            margin: 0 auto 1.5rem;
            font-size: 1.5rem;
            transition: all 0.3s ease;
        }

        .card:hover .icon-circle {
            background-color: var(--propload-secondary);
            color: #fff;
        }

        .fade-in {
            opacity: 0;
            animation: fadeIn 0.6s ease-out forwards;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .back-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 42px;
            height: 42px;
            background-color: var(--surface);
            border-radius: 50%;
            box-shadow: var(--card-shadow);
            color: var(--propload-secondary);
            transition: all 0.3s ease;
            border: 1px solid #e5e7eb;
            position: absolute;
            top: 1.5rem;
            left: 1.5rem;
        }

        .back-btn:hover {
            background-color: var(--propload-secondary);
            color: white;
            transform: translateY(-2px);
        }

        @media (max-width: 640px) {
            .back-btn {
                top: 1rem;
                left: 1rem;
                width: 38px;
                height: 38px;
            }
        }
    </style>
</head>

<body class="bg-gray-50">
    <%- include('./sideNav') %>
    <main class="w-full">

        <div class="max-w-4xl mx-auto px-4 py-8">
            <%- include('../partials/flash-messages') %>

            <div class="flex justify-between items-center mb-8">
                <h1 class="text-4xl md:text-5xl font-extrabold text-secondary mb-3 tracking-tight">My Profile</h1>
                <a href="/customer/dashboard"
                    class="bg-secondary text-white px-4 py-2 rounded hover:opacity-90 transition duration-300">
                    Back to Dashboard
                </a>
            </div>

            <div class="bg-white rounded-lg shadow p-6">
                <div class="flex items-center space-x-6 mb-8 pb-6 border-b border-gray-200">
                    <div class="relative">
                        <% if (profile.profileImage && profile.profileImage.url) { %>
                            <img src="<%= profile.profileImage.url %>" alt="<%= profile.name %>"
                                class="w-24 h-24 rounded-full object-cover border-4 border-white shadow-lg" />
                            <% } else { %>
                                <div
                                    class="w-24 h-24 bg-gray-300 rounded-full flex items-center justify-center border-4 border-white shadow-lg">
                                    <i class="fas fa-user text-gray-600 text-3xl"></i>
                                </div>
                                <% } %>
                    </div>
                    <div>
                        <h2 class="text-2xl font-bold text-secondary"><%= profile.name %></h2>
                        <p class="text-gray-600"><%= profile.email %></p>
                        <span
                            class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-secondary text-white capitalize mt-2">
                            <%= profile.role %>
                        </span>
                    </div>
                </div>

                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <h3 class="text-lg font-semibold text-secondary mb-4">Personal Information</h3>
                        <div class="space-y-3">
                            <div>
                                <label class="block text-sm font-medium text-secondary">Full Name</label>
                                <p class="mt-1 text-gray-900"><%= profile.name %></p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-secondary">Email Address</label>
                                <p class="mt-1 text-gray-900"><%= profile.email %></p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-secondary">Phone Number</label>
                                <p class="mt-1 text-gray-900"><%= profile.phone %></p>
                            </div>
                            <div>
                                <label class="block text-sm font-medium text-secondary">Role</label>
                                <p class="mt-1 text-gray-900 capitalize"><%= profile.role %></p>
                            </div>
                        </div>
                    </div>

                    <div>
                        <h3 class="text-lg font-semibold text-secondary mb-4">Account Status</h3>
                        <div class="space-y-3">
                            <div>
                                <label class="block text-sm font-medium text-secondary">Member Since</label>
                                <p class="mt-1 text-gray-900">
                                    <%= new Date(profile.createdAt).toLocaleDateString() %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="mt-8 pt-6 border-t border-gray-200">
                    <h3 class="text-lg font-semibold text-secondary mb-4">Update Profile Image</h3>
                    <form action="/customer/profile/image" method="POST" enctype="multipart/form-data"
                        class="flex items-center space-x-4">
                        <input type="file" name="profileImage" accept="image/*" required
                            class="flex-1 text-sm text-secondary file:mr-4 file:py-2 file:px-4 file:rounded-md file:border-0 file:text-sm file:font-semibold file:bg-primary file:text-secondary hover:file:opacity-90" />
                        <button type="submit"
                            class="bg-secondary text-white px-4 py-2 rounded hover:opacity-90 transition duration-300">
                            Upload Image
                        </button>
                    </form>
                </div>

                <!-- ✅ UPDATED SECTION -->
                <div class="mt-8 pt-6 border-t border-gray-200">
                    <h3 class="text-lg font-semibold text-secondary mb-4">Quick Actions</h3>
                    <div class="grid md:grid-cols-2 gap-4">
                        <button
                            class="bg-secondary text-white w-full px-4 py-3 rounded hover:opacity-90 transition duration-300"
                            onclick="window.location.href='/customer/profile/edit'">
                            Edit Profile
                        </button>

                        <button
                            class="bg-primary text-secondary w-full px-4 py-3 rounded hover:opacity-90 border border-secondary transition duration-300"
                            onclick="window.location.href='/customer/change-password'">
                            Change Password
                        </button>
                    </div>
                </div>
                <!-- ✅ END UPDATED SECTION -->

            </div>
        </div>
    </main>
</body>

</html>
