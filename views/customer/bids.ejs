<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Bids - Propload</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>

    <div class="max-w-7xl mx-auto px-4 py-8">
        <%- include('../partials/flash-messages') %>

        <div class="flex justify-between items-center mb-8">
            <h1 class="text-3xl font-bold text-gray-900">My Bids</h1>
            <a href="/customer/dashboard" class="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700">
                Back to Dashboard
            </a>
        </div>

        <% if (bids && bids.length > 0) { %>
            <div class="grid gap-6">
                <% bids.forEach(bid => { %>
                <div class="bg-white rounded-lg shadow p-6">
                    <div class="flex justify-between items-start mb-4">
                        <div class="flex-1">
                            <h3 class="text-xl font-semibold text-gray-900">
                                <%= bid.project.title %>
                            </h3>
                            <p class="text-gray-600">Category: <%= bid.project.category %></p>
                        </div>
                        <div class="text-right">
                            <p class="text-2xl font-bold text-green-600">$<%= bid.amount.toLocaleString() %></p>
                            <span class="inline-block px-3 py-1 rounded-full text-sm font-medium 
                                <%= bid.status === 'submitted' ? 'bg-blue-100 text-blue-800' : '' %>
                                <%= bid.status === 'won' ? 'bg-green-100 text-green-800' : '' %>
                                <%= bid.status === 'lost' ? 'bg-red-100 text-red-800' : '' %>">
                                <%= bid.status.toUpperCase() %>
                            </span>
                        </div>
                    </div>
                    
                    <div class="grid md:grid-cols-2 gap-4 mb-4">
                        <div>
                            <h4 class="font-semibold text-gray-900 mb-2">Seller Information</h4>
                            <!-- UPDATED: Seller info with profile image -->
                            <div class="flex items-center space-x-3 mb-3">
                                <% if (bid.seller.profileImage && bid.seller.profileImage.url) { %>
                                <img src="<%= bid.seller.profileImage.url %>" 
                                     alt="<%= bid.seller.companyName || bid.seller.name %>"
                                     class="w-12 h-12 rounded-full object-cover">
                                <% } else { %>
                                <div class="w-12 h-12 bg-gray-300 rounded-full flex items-center justify-center">
                                    <i class="fas fa-user text-gray-600"></i>
                                </div>
                                <% } %>
                                <div>
                                    <p class="text-gray-700 font-medium"><%= bid.seller.companyName || bid.seller.name %></p>
                                    <p class="text-gray-600 text-sm"><%= bid.seller.email %></p>
                                </div>
                            </div>
                            <p class="text-gray-600"><%= bid.seller.phone %></p>
                            <% if (bid.seller.ratings) { %>
                            <p class="text-gray-600">Rating: <%= bid.seller.ratings.average %>/5 (<%= bid.seller.ratings.count %> reviews)</p>
                            <% } %>
                        </div>
                        <div>
                            <h4 class="font-semibold text-gray-900 mb-2">Bid Details</h4>
                            <p class="text-gray-700">Submitted: <%= new Date(bid.createdAt).toLocaleDateString() %></p>
                            <% if (bid.timeline && bid.timeline.duration) { %>
                            <p class="text-gray-700">Timeline: <%= bid.timeline.duration %> days</p>
                            <% } %>
                            
                            <!-- NEW: Bid Attachments -->
                            <% if (bid.attachments && bid.attachments.length > 0) { %>
                            <div class="mt-3">
                                <p class="font-medium text-gray-700 mb-1">Attachments:</p>
                                <div class="space-y-1">
                                    <% bid.attachments.forEach(attachment => { %>
                                    <a href="<%= attachment.url %>" target="_blank" 
                                       class="flex items-center text-sm text-blue-600 hover:text-blue-800">
                                        <i class="fas fa-paperclip mr-1"></i>
                                        <%= attachment.originalName || 'Document' %>
                                        <span class="text-gray-500 text-xs ml-2">
                                            (<%= (attachment.bytes / 1024 / 1024).toFixed(2) %> MB)
                                        </span>
                                    </a>
                                    <% }) %>
                                </div>
                            </div>
                            <% } %>
                        </div>
                    </div>

                    <% if (bid.proposal) { %>
                    <div class="mb-4">
                        <h4 class="font-semibold text-gray-900 mb-2">Proposal</h4>
                        <p class="text-gray-700"><%= bid.proposal %></p>
                    </div>
                    <% } %>

                    <% if (bid.status === 'submitted') { %>
                    <div class="flex space-x-4">
                        <form action="/customer/select-bid/<%= bid._id %>" method="POST" class="inline">
                            <button type="submit" 
                                    onclick="return confirm('Are you sure you want to select this bid?')"
                                    class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition duration-300">
                                Select This Bid
                            </button>
                        </form>
                        <a href="/customer/download-contract/<%= bid._id %>" 
                           class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition duration-300">
                            Download Contract
                        </a>
                    </div>
                    <% } %>
                </div>
                <% }) %>
            </div>
        <% } else { %>
            <div class="text-center py-12">
                <i class="fas fa-gavel text-6xl text-gray-300 mb-4"></i>
                <h3 class="text-xl font-semibold text-gray-900 mb-2">No Bids Yet</h3>
                <p class="text-gray-600 mb-6">Bids from sellers will appear here when they respond to your projects.</p>
                <a href="/customer/add-project" class="bg-indigo-600 text-white px-6 py-3 rounded-lg hover:bg-indigo-700">
                    Create Your First Project
                </a>
            </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>