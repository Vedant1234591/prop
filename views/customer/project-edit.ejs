<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Project - Propload</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>

    <div class="max-w-4xl mx-auto px-4 py-8">
        <%- include('../partials/flash-messages') %>

        <div class="bg-white shadow rounded-lg p-8">
            <div class="flex justify-between items-center mb-6">
                <h1 class="text-3xl font-bold text-gray-900">Edit Project</h1>
                <div class="text-right">
                    <span class="px-4 py-2 bg-yellow-100 text-yellow-800 rounded-full text-sm font-medium">
                        <%= project.adminStatus === 'rejected' || project.adminStatus === 'defected' ? 'NEEDS CORRECTION' : 'DRAFTED - Editable' %>
                    </span>
                    <% if (project.adminStatus === 'rejected' || project.adminStatus === 'defected') { %>
                    <p class="text-xs text-red-600 mt-1">Please correct and resubmit for verification</p>
                    <% } %>
                </div>
            </div>

            <!-- Admin Remarks (if rejected/defected) -->
            <% if (project.adminRemarks && (project.adminStatus === 'rejected' || project.adminStatus === 'defected')) { %>
            <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
                <h3 class="text-lg font-semibold text-red-800 mb-2">Admin Remarks</h3>
                <p class="text-red-700"><%= project.adminRemarks %></p>
                <p class="text-sm text-red-600 mt-2">Please address these concerns and resubmit for verification.</p>
            </div>
            <% } %>

            <!-- Regular Update Form -->
            <form id="updateForm" action="/customer/update-project/<%= project._id %>" method="POST" enctype="multipart/form-data" class="space-y-6">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <input type="hidden" name="resubmit" id="resubmitField" value="false">
                
                <div class="grid md:grid-cols-2 gap-6">
                    <div>
                        <label for="title" class="block text-sm font-medium text-gray-700">Project Title *</label>
                        <input type="text" id="title" name="title" required 
                               value="<%= project.title %>"
                               class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                    </div>

                    <div>
                        <label for="phone" class="block text-sm font-medium text-gray-700">Contact Phone *</label>
                        <input type="tel" id="phone" name="phone" required 
                               value="<%= project.contact.phone %>"
                               class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                    </div>
                </div>

                <div>
                    <label for="description" class="block text-sm font-medium text-gray-700">Project Description *</label>
                    <textarea id="description" name="description" rows="4" required
                              class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"><%= project.description %></textarea>
                </div>

                <div>
                    <label for="requirements" class="block text-sm font-medium text-gray-700">Detailed Requirements *</label>
                    <textarea id="requirements" name="requirements" rows="6" required
                              class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500"><%= project.requirements %></textarea>
                </div>

                <div class="border-t pt-6">
                    <h3 class="text-lg font-medium text-gray-900 mb-4">Location Details</h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="address" class="block text-sm font-medium text-gray-700">Address *</label>
                            <input type="text" id="address" name="address" required 
                                   value="<%= project.location.address %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>

                        <div>
                            <label for="city" class="block text-sm font-medium text-gray-700">City *</label>
                            <input type="text" id="city" name="city" required 
                                   value="<%= project.location.city %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>

                        <div>
                            <label for="state" class="block text-sm font-medium text-gray-700">State *</label>
                            <input type="text" id="state" name="state" required 
                                   value="<%= project.location.state %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>

                        <div>
                            <label for="zipCode" class="block text-sm font-medium text-gray-700">ZIP Code *</label>
                            <input type="text" id="zipCode" name="zipCode" required 
                                   value="<%= project.location.zipCode %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                    </div>
                </div>

                <!-- Category-specific fields -->
                <% if (category === 'electrification') { %>
                <div class="border-t pt-6">
                    <h3 class="text-lg font-medium text-gray-900 mb-4">Electrical Specifications</h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="voltage" class="block text-sm font-medium text-gray-700">Voltage Requirements</label>
                            <input type="text" id="voltage" name="specifications[voltage]" 
                                   value="<%= project.specifications.get('voltage') || '' %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                        <div>
                            <label for="wiringType" class="block text-sm font-medium text-gray-700">Wiring Type</label>
                            <input type="text" id="wiringType" name="specifications[wiringType]" 
                                   value="<%= project.specifications.get('wiringType') || '' %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                    </div>
                </div>
                <% } else if (category === 'architecture') { %>
                <div class="border-t pt-6">
                    <h3 class="text-lg font-medium text-gray-900 mb-4">Architectural Specifications</h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="buildingType" class="block text-sm font-medium text-gray-700">Building Type</label>
                            <input type="text" id="buildingType" name="specifications[buildingType]" 
                                   value="<%= project.specifications.get('buildingType') || '' %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                        <div>
                            <label for="floorArea" class="block text-sm font-medium text-gray-700">Floor Area (sq ft)</label>
                            <input type="number" id="floorArea" name="specifications[floorArea]" 
                                   value="<%= project.specifications.get('floorArea') || '' %>"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                        </div>
                    </div>
                </div>
                <% } %>

                <!-- Current Images Section -->
                <% if (project.images && project.images.length > 0) { %>
                <div class="border-t pt-6">
                    <h3 class="text-lg font-medium text-gray-900 mb-4">Current Images</h3>
                    <div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-4">
                        <% project.images.forEach((image, index) => { %>
                        <div class="relative group">
                            <img src="<%= image.url %>" 
                                 alt="Project image <%= index + 1 %>" 
                                 class="w-full h-24 object-cover rounded-lg shadow-sm">
                            <form action="/customer/project/<%= project._id %>/remove-image/<%= image.public_id %>" method="POST" class="absolute top-1 right-1">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button type="submit" 
                                        onclick="return confirm('Are you sure you want to remove this image?')"
                                        class="bg-red-600 text-white p-1 rounded-full opacity-0 group-hover:opacity-100 transition duration-300">
                                    <i class="fas fa-times text-xs"></i>
                                </button>
                            </form>
                        </div>
                        <% }) %>
                    </div>
                </div>
                <% } %>

                <!-- Add Images Section -->
                <div class="border-t pt-6">
                    <h3 class="text-lg font-medium text-gray-900 mb-4">Add More Images</h3>
                    <div class="grid md:grid-cols-2 gap-6">
                        <div>
                            <label for="images" class="block text-sm font-medium text-gray-700">Project Images</label>
                            <input type="file" id="images" name="images" multiple accept="image/*"
                                   class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500">
                            <p class="text-xs text-gray-500 mt-1">Upload additional images for your project</p>
                        </div>
                    </div>
                </div>

                <div class="flex justify-between pt-6 border-t">
                    <a href="/customer/my-projects" 
                       class="bg-gray-300 text-gray-700 px-6 py-2 rounded-md hover:bg-gray-400 transition duration-300">
                        Cancel
                    </a>
                    <div class="flex space-x-4">
                        <!-- Allow deletion for both drafted and defected projects -->
                        <% if (project.status === 'drafted' || project.status === 'defected') { %>
                        <form action="/customer/delete-project/<%= project._id %>" method="POST" 
                              onsubmit="return confirm('Are you sure you want to delete this project? This action cannot be undone.')">
                            <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                            <button type="submit" 
                                    class="bg-red-600 text-white px-6 py-2 rounded-md hover:bg-red-700 transition duration-300">
                                Delete Project
                            </button>
                        </form>
                        <% } %>
                        
                        <!-- Single submit button that changes behavior based on which button is clicked -->
                        <button type="button" id="updateBtn" 
                                class="bg-indigo-600 text-white px-6 py-2 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 transition duration-300">
                            Update Project
                        </button>
                        
                        <!-- Resubmit button for rejected/defected projects -->
                        <% if (project.adminStatus === 'rejected' || project.adminStatus === 'defected') { %>
                        <button type="button" id="resubmitBtn"
                                class="bg-green-600 text-white px-6 py-2 rounded-md hover:bg-green-700 transition duration-300">
                            Update & Resubmit
                        </button>
                        <% } %>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const updateForm = document.getElementById('updateForm');
            const resubmitField = document.getElementById('resubmitField');
            const updateBtn = document.getElementById('updateBtn');
            const resubmitBtn = document.getElementById('resubmitBtn');

            // Handle regular update
            if (updateBtn) {
                updateBtn.addEventListener('click', function() {
                    resubmitField.value = 'false';
                    updateForm.submit();
                });
            }

            // Handle update and resubmit
            if (resubmitBtn) {
                resubmitBtn.addEventListener('click', function() {
                    resubmitField.value = 'true';
                    updateForm.submit();
                });
            }
        });
    </script>

    <%- include('../partials/footer') %>
</body>
</html>