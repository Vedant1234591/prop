<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add Project - Propload</title>

    <!-- Tailwind & FontAwesome -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />

    <style>
      :root {
        --propload-primary: #e9e2b3;
        --propload-secondary: #2f6c7b;
        --surface: #ffffff;
        --muted: #6b7280;
        --card-shadow: 0 8px 20px rgba(47, 76, 86, 0.06);
      }

      body {
        font-family: "Inter", sans-serif;
        background-color: #f9fafb;
        color: var(--muted);
        display: flex;
        min-height: 100vh;
      }

      main {
        flex: 1;
        padding: 2rem 1rem;
        margin-left: 260px; /* Sidebar width */
        transition: margin-left 0.3s ease;
      }

      @media (max-width: 1024px) {
        main {
          margin-left: 0;
          padding: 1.5rem;
        }
      }

      .text-primary {
        color: var(--propload-primary);
      }

      .text-secondary {
        color: var(--propload-secondary);
      }

      .bg-primary {
        background-color: var(--propload-primary);
      }

      .bg-secondary {
        background-color: var(--propload-secondary);
      }

      .card {
        background: var(--surface);
        border-radius: 1.25rem;
        box-shadow: var(--card-shadow);
        transition: all 0.3s ease;
        border: 1px solid transparent;
      }

      .card:hover {
        transform: translateY(-6px);
        border-color: var(--propload-secondary);
        box-shadow: 0 12px 28px rgba(47, 108, 123, 0.15);
      }

      .icon-circle {
        width: 5rem;
        height: 5rem;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        background-color: var(--propload-primary);
        color: var(--propload-secondary);
        margin: 0 auto 1.5rem;
        font-size: 1.5rem;
        transition: all 0.3s ease;
      }

      .card:hover .icon-circle {
        background-color: var(--propload-secondary);
        color: #fff;
      }

      .fade-in {
        opacity: 0;
        animation: fadeIn 0.6s ease-out forwards;
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(10px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Back button */
      .back-btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        width: 42px;
        height: 42px;
        background-color: var(--surface);
        border-radius: 50%;
        box-shadow: var(--card-shadow);
        color: var(--propload-secondary);
        transition: all 0.3s ease;
        border: 1px solid #e5e7eb;
        position: absolute;
        top: 1.5rem;
        left: 1.5rem;
      }

      .back-btn:hover {
        background-color: var(--propload-secondary);
        color: white;
        transform: translateY(-2px);
      }

      @media (max-width: 640px) {
        .back-btn {
          top: 1rem;
          left: 1rem;
          width: 38px;
          height: 38px;
        }
      }
    </style>
  </head>

  <body class="antialiased">
    <%- include('../partials/flash-messages') %>
    <%- include('./sideNav') %>

    <main class="fade-in relative">
      <!-- Back Arrow -->
      <a href="/customer/dashboard" class="back-btn">
        <i class="fas fa-arrow-left text-lg"></i>
      </a>

      <!-- Header -->
      <div class="text-center mb-14 mt-4">
        <h1 class="text-4xl md:text-5xl font-extrabold text-secondary mb-3 tracking-tight">
          Start Your Project
        </h1>
        <p class="text-base md:text-lg text-gray-600 max-w-2xl mx-auto">
          Choose a category that best matches your project and begin your journey with
          Propload’s trusted professionals.
        </p>
        <div class="w-20 h-1 bg-primary mx-auto mt-4 rounded-full"></div>
      </div>

      <!-- 2x2 Card Grid -->
      <div class="grid grid-cols-1 sm:grid-cols-2 gap-8 max-w-5xl mx-auto">
        <!-- Electrification -->
        <div class="card p-8 text-center">
          <div class="icon-circle">
            <i class="fas fa-bolt"></i>
          </div>
          <h3 class="text-2xl font-semibold text-secondary mb-3">Electrification</h3>
          <p class="mb-6 text-gray-600">
            Electrical systems, wiring, lighting, and power distribution solutions.
          </p>
          <a
            href="/customer/project-form/electrification"
            class="bg-secondary text-white px-8 py-3 rounded-lg hover:opacity-90 transition duration-300 inline-block font-medium"
          >
            Build Now
          </a>
        </div>

        <!-- Architecture -->
        <div class="card p-8 text-center">
          <div class="icon-circle">
            <i class="fas fa-building"></i>
          </div>
          <h3 class="text-2xl font-semibold text-secondary mb-3">Architecture</h3>
          <p class="mb-6 text-gray-600">
            Building design, structural planning, and creative architecture solutions.
          </p>
          <a
            href="/customer/project-form/architecture"
            class="bg-secondary text-white px-8 py-3 rounded-lg hover:opacity-90 transition duration-300 inline-block font-medium"
          >
            Build Now
          </a>
        </div>

        <!-- Interior Design -->
        <div class="card p-8 text-center">
          <div class="icon-circle">
            <i class="fas fa-couch"></i>
          </div>
          <h3 class="text-2xl font-semibold text-secondary mb-3">Interior Design</h3>
          <p class="mb-6 text-gray-600">
            Space planning, furniture design, and interior styling for modern spaces.
          </p>
          <a
            href="/customer/project-form/interior-design"
            class="bg-secondary text-white px-8 py-3 rounded-lg hover:opacity-90 transition duration-300 inline-block font-medium"
          >
            Build Now
          </a>
        </div>

        <!-- General Construction -->
        <div class="card p-8 text-center">
          <div class="icon-circle">
            <i class="fas fa-tools"></i>
          </div>
          <h3 class="text-2xl font-semibold text-secondary mb-3">General Construction</h3>
          <p class="mb-6 text-gray-600">
            Construction, renovation, and contracting services to build with precision.
          </p>
          <a
            href="/customer/project-form/general-construction"
            class="bg-secondary text-white px-8 py-3 rounded-lg hover:opacity-90 transition duration-300 inline-block font-medium"
          >
            Build Now
          </a>
        </div>
      </div>
    </main>
  </body>
</html>
