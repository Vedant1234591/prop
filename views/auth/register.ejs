<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
     <link rel="icon" type="image/png" href="/images/landing/Vector.jpeg">
    <title>Register - Propload</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css"
      rel="stylesheet"
    />

    <style>
      .bg-propload {
        background-color: #e9e2b3;
      }
      .bg-propload:hover {
        background-color: #dcd39e;
      }
    </style>
  </head>

  <body class="bg-white min-h-screen flex flex-col">
    <!-- Header -->
    <div class="shadow-sm w-full ">
      <%- include('../partials/landingHeader') %>
    </div>

    <!-- ✅ Split Section -->
    <div class="flex flex-col md:flex-row w-full min-h-screen">
      <!-- Left Image Section -->
      <!-- Left Image Section (using your local image) -->
      <div
        class="hidden md:flex md:w-1/2 items-center justify-center relative p-16"
      >
        <img
          src="/images/login/signup.webp"
          alt="Construction Crane"
          class="max-w-md w-full h-auto rounded-2xl object-contain"
        />
      </div>

      <!-- Right Form Section -->
      <div
        class="w-full md:w-1/2 flex flex-col justify-center px-8 py-12 md:px-16"
      >
        <div class="max-w-lg mx-auto w-full">
          <div class="text-center mb-8">
            <h2 class="mt-4 text-2xl font-bold text-gray-900">
              Join as <%= role === 'seller' ? 'Service Provider' : 'Customer' %>
            </h2>
          </div>

          <%- include('../partials/flash-messages') %>

          <div class="bg-white shadow rounded-lg p-8 w-50%">
            <form action="/auth/register" method="POST" class="space-y-6">
              <input type="hidden" name="role" value="<%= role %>" />

              <div class="grid md:grid-cols-2 gap-6">
                <div>
                  <label
                    for="name"
                    class="block text-sm font-medium text-gray-700"
                    >Full Name *</label
                  >
                  <input
                    type="text"
                    id="name"
                    name="name"
                    required
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>

                <div>
                  <label
                    for="email"
                    class="block text-sm font-medium text-gray-700"
                    >Email Address *</label
                  >
                  <input
                    type="email"
                    id="email"
                    name="email"
                    required
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>

                <div>
                  <label
                    for="phone"
                    class="block text-sm font-medium text-gray-700"
                    >Phone Number *</label
                  >
                  <input
                    type="tel"
                    id="phone"
                    name="phone"
                    required
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>

                <% if (role === 'seller') { %>
                <div>
                  <label
                    for="companyName"
                    class="block text-sm font-medium text-gray-700"
                    >Company Name *</label
                  >
                  <input
                    type="text"
                    id="companyName"
                    name="companyName"
                    required
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>
                <% } %>

                <div>
                  <label
                    for="password"
                    class="block text-sm font-medium text-gray-700"
                    >Password *</label
                  >
                  <input
                    type="password"
                    id="password"
                    name="password"
                    required
                    minlength="6"
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>

                <div>
                  <label
                    for="confirmPassword"
                    class="block text-sm font-medium text-gray-700"
                    >Confirm Password *</label
                  >
                  <input
                    type="password"
                    id="confirmPassword"
                    name="confirmPassword"
                    required
                    minlength="6"
                    class="mt-1 block w-full border border-gray-300 rounded-md py-2 px-3 focus:ring-indigo-500 focus:border-indigo-500"
                  />
                </div>
              </div>

              <!-- <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
                <p class="text-sm text-blue-700">
                  <dialog>a</dialog>
                  By registering, you agree to our Terms of Service and Privacy
                  Policy.
                </p>
              </div> -->

              <style>
                .Sign {
                  /* color: #dcd3bf; */
                  /* color: rgb(49, 94, 113); */
                }
                .Sign:hover {
                  color: rgb(49, 94, 113);
                }
              </style>

              <div class="flex items-start space-x-2 text-sm text-blue-700">
                <input
                  required
                  id="terms"
                  type="checkbox"
                  class="mt-1 h-4 w-4 rounded border-gray-300 text-blue-600 focus:ring-blue-500"
                />
                <label for="terms" class="leading-tight">
                  By registering, you agree to our
                  <a href="/auth/terms" class="underline hover:text-blue-800"
                    >Terms of Service</a
                  >
                  and
                  <a href="/auth/privacy" class="underline hover:text-blue-800"
                    >Privacy Policy</a
                  >.
                </label>
              </div>
              <div class="flex items-center justify-between">
                <div>
                  Already have an account?
                  <a href="/auth/login" class="text-gray-600 text-md Sign">
                    Sign in
                  </a>
                </div>

                <button
                  type="submit"
                  class="bg-propload text-gray-900 px-6 py-2 rounded-md hover:bg-indigo-700 hover:text-white transition duration-300"
                >
                  <%= role === 'seller' ? 'Next' : 'Create Account' %>
                </button>
              </div>
            </form>
          </div>

          <div class="text-center mt-6">
            <p class="text-gray-600">
              Want to join as <%= role === 'seller' ? 'customer' :
              'service_provider' %>?
              <a
                href="/auth/register?role=<%= role === 'seller' ? 'customer' : 'seller' %>"
                class="text-indigo-600 hover:text-indigo-500"
              >
                Click here
              </a>
            </p>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
