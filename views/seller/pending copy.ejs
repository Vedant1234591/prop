<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Waiting for Admin Approval - Propload</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
      rel="stylesheet"
    />

    <style>
      :root {
        --propload-primary: #e9e2b3;
        --propload-secondary: rgb(49, 94, 113);
      }

      body {
        font-family: "Inter", "Segoe UI", sans-serif;
        background-color: #f8f7f2;
        color: #1f2937;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .approval-wrapper {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 2rem;
      }

      .approval-card {
        background: white;
        border-radius: 1rem;
        padding: 3rem;
        width: 100%;
        max-width: 600px;
        text-align: center;
        box-shadow: 0 4px 20px rgba(49, 94, 113, 0.08);
        transition: box-shadow 0.3s ease, transform 0.2s ease;
      }

      .approval-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 6px 25px rgba(49, 94, 113, 0.12);
      }

      .icon-circle {
        width: 80px;
        height: 80px;
        border-radius: 50%;
        background-color: var(--propload-secondary);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-size: 2rem;
        margin: 0 auto 1.5rem;
      }

      h1 {
        font-size: 1.8rem;
        font-weight: 700;
        margin-bottom: 0.75rem;
      }

      p {
        color: #4b5563;
        font-size: 1rem;
        line-height: 1.6;
        margin: 0.5rem 0;
      }

      .eta {
        color: var(--propload-secondary);
        font-weight: 600;
        margin-top: 0.5rem;
      }

      .subtle-line {
        width: 60px;
        height: 3px;
        border-radius: 3px;
        background-color: var(--propload-secondary);
        margin: 1rem auto 1.5rem;
      }

      .note {
        color: #6b7280;
        font-size: 0.95rem;
        margin-top: 1rem;
      }

      .contact-btn {
        margin-top: 1.8rem;
        background-color: var(--propload-secondary);
        color: white;
        padding: 0.65rem 1.6rem;
        border-radius: 0.5rem;
        text-decoration: none;
        font-weight: 500;
        display: inline-block;
        transition: background-color 0.3s ease;
      }

      .contact-btn:hover {
        background-color: rgb(42, 81, 97);
      }
    </style>
  </head>

  <body>
    <!-- Header -->
    <div class="shadow-sm w-full px-20 bg-white">
      <%- include('../partials/landingHeader') %>
    </div>

    <!-- Main -->
    <div class="approval-wrapper">
      <div class="approval-card">
        <div class="icon-circle">
          <i class="bi bi-hourglass-split"></i>
        </div>

        <h1>Waiting for Admin Approval</h1>
        <div class="subtle-line"></div>

        <p>
          Thank you for registering with <strong>Propload</strong>. Your seller
          profile is currently under review by our verification team.
        </p>

        <% if (typeof hours !== 'undefined') { %>
        <p class="eta">
          Estimated remaining time:
          <strong><%= hours %> hour(s) <%= minutes %> minute(s)</strong>
        </p>
        <% } else { %>
        <p class="eta">Typical processing time: up to 48 hours</p>
        <% } %>

        <p class="note">
          Once your verification is complete, you'll gain access to your seller
          dashboard. We’ll notify you via email when your account is approved.
        </p>

        <a href="/support" class="contact-btn">Contact Support</a>
      </div>
    </div>
  </body>
</html>
