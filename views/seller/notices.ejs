<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notices - Propload</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50">
    <%- include('../partials/header') %>

    <div class="max-w-4xl mx-auto px-4 py-8">
        <%- include('../partials/flash-messages') %>

        <div class="flex justify-between items-center mb-8">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Notices & Updates</h1>
                <p class="text-gray-600 mt-2">Stay updated with the latest announcements and important information.</p>
            </div>
            <div class="text-sm text-gray-500">
                <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full">
                    <%= notices.length %> Notices
                </span>
            </div>
        </div>

        <!-- Notices List -->
        <div class="space-y-6">
            <% if (notices.length > 0) { %>
                <% notices.forEach(notice => { %>
                <div class="bg-white shadow rounded-lg overflow-hidden hover:shadow-lg transition duration-300">
                    <div class="border-l-4 <%= notice.priority === 'high' ? 'border-red-500' : notice.priority === 'medium' ? 'border-orange-500' : 'border-blue-500' %>">
                        <div class="p-6">
                            <div class="flex justify-between items-start mb-3">
                                <h3 class="text-xl font-semibold text-gray-900"><%= notice.title %></h3>
                                <div class="flex items-center space-x-2">
                                    <% if (notice.priority === 'high') { %>
                                        <span class="px-2 py-1 bg-red-100 text-red-800 text-xs rounded-full">URGENT</span>
                                    <% } else if (notice.priority === 'medium') { %>
                                        <span class="px-2 py-1 bg-orange-100 text-orange-800 text-xs rounded-full">IMPORTANT</span>
                                    <% } else { %>
                                        <span class="px-2 py-1 bg-blue-100 text-blue-800 text-xs rounded-full">GENERAL</span>
                                    <% } %>
                                    <% if (notice.type === 'urgent') { %>
                                        <span class="px-2 py-1 bg-red-100 text-red-800 text-xs rounded-full">ALERT</span>
                                    <% } %>
                                </div>
                            </div>
                            
                            <div class="prose max-w-none text-gray-700 mb-4">
                                <p class="whitespace-pre-wrap"><%= notice.content %></p>
                            </div>
                            
                            <div class="flex justify-between items-center text-sm text-gray-500 pt-4 border-t border-gray-100">
                                <div class="flex items-center space-x-4">
                                    <span>
                                        <i class="fas fa-calendar mr-1"></i>
                                        <%= new Date(notice.createdAt).toLocaleDateString('en-US', { 
                                            year: 'numeric', 
                                            month: 'long', 
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </span>
                                    <% if (notice.endDate) { %>
                                    <span>
                                        <i class="fas fa-clock mr-1"></i>
                                        Expires: <%= new Date(notice.endDate).toLocaleDateString() %>
                                    </span>
                                    <% } %>
                                </div>
                                <span class="capitalize"><%= notice.type %> notice</span>
                            </div>
                        </div>
                    </div>
                </div>
                <% }) %>
            <% } else { %>
                <div class="bg-white shadow rounded-lg p-12 text-center">
                    <i class="fas fa-bell text-gray-300 text-6xl mb-4"></i>
                    <h3 class="text-lg font-medium text-gray-900 mb-2">No notices available</h3>
                    <p class="text-gray-500">There are no notices at the moment. Check back later for updates.</p>
                </div>
            <% } %>
        </div>

        <!-- Pagination (if needed in future) -->
        <% if (totalPages > 1) { %>
        <div class="flex justify-center items-center space-x-2 mt-8">
            <% if (currentPage > 1) { %>
                <a href="?page=<%= currentPage - 1 %>" class="px-4 py-2 bg-white border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50 transition duration-300">
                    Previous
                </a>
            <% } %>
            
            <% for (let i = 1; i <= totalPages; i++) { %>
                <a href="?page=<%= i %>" class="px-4 py-2 <%= currentPage === i ? 'bg-indigo-600 text-white' : 'bg-white text-gray-700 border border-gray-300' %> rounded-md hover:bg-indigo-50 transition duration-300">
                    <%= i %>
                </a>
            <% } %>
            
            <% if (currentPage < totalPages) { %>
                <a href="?page=<%= currentPage + 1 %>" class="px-4 py-2 bg-white border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50 transition duration-300">
                    Next
                </a>
            <% } %>
        </div>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>