<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= blog.title %> - Propload</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        .btn-primary-custom {
            background-color: #40788e;
            color: white;
        }
        .btn-primary-custom:hover {
            background-color: #2f5e6b;
            color: white;
        }
        .blog-content {
            line-height: 1.8;
            font-size: 1.1rem;
            color: #333;
        }
        .blog-content p {
            margin-bottom: 1.5rem;
        }
        .blog-meta {
            color: #6c757d;
            font-size: 0.9rem;
        }
    </style>
</head>
<body>
    <%- include('./partials/landingHeader') %>

    <div class="container py-5 mt-5">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <!-- Back button -->
                <a href="/" class="btn btn-outline-secondary mb-4">
                    <i class="fas fa-arrow-left me-2"></i> Back to Home
                </a>

                <!-- Blog Image -->
                <% if (blog.img) { %>
                <img 
                    src="<%= blog.img %>" 
                    alt="<%= blog.title %>" 
                    class="img-fluid rounded mb-4 w-100"
                    style="height: 400px; object-fit: cover;"
                >
                <% } %>

                <!-- Blog Meta -->
                <div class="blog-meta d-flex justify-content-between align-items-center mb-3">
                    <span>
                        <i class="fas fa-calendar me-1"></i>
                        <%= blog.formattedDate || blog.date.toLocaleDateString() %>
                    </span>
                    <span>
                        <i class="fas fa-user me-1"></i>
                        By <%= blog.author || 'Admin' %>
                    </span>
                </div>

                <!-- Blog Title -->
                <h1 class="mb-4" style="color: #2f6c7b;"><%= blog.title %></h1>

                <!-- Blog Summary -->
                <% if (blog.summary) { %>
                <div class="alert alert-light border mb-4">
                    <strong>Summary:</strong> <%= blog.summary %>
                </div>
                <% } %>

                <!-- Blog Content -->
                <div class="blog-content mb-5">
                    <% if (blog.content) { %>
                        <%= blog.content.replace(/\n/g, '<br>') %>
                    <% } else { %>
                        <p class="text-muted">No content available for this blog post.</p>
                    <% } %>
                </div>

                <hr class="my-5">

                <!-- Navigation -->
                <div class="d-flex justify-content-between flex-wrap gap-3">
                    <a href="/" class="btn btn-primary-custom">
                        <i class="fas fa-home me-2"></i> Back to Home
                    </a>
                    <a href="/#blog-section" class="btn btn-outline-secondary">
                        View All Blogs <i class="fas fa-arrow-right ms-2"></i>
                    </a>
                </div>
            </div>
        </div>
    </div>

    <%- include('./partials/landingFooter') %>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>